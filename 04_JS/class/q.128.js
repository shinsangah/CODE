class HydroCar extends Car {
  constructor(brand, color, fuel) { }
  super(brand, color);
  this.fuel = fuel;
}

showSpec() {
  super.showSpec();
  console.log(`그리고 이 차는 ${this.fuel}의 힘으로 움직입니다!`);
}
}

const hyundai = new Car('hyundai', 'white');
const tesla = new ElecCar('tesla', 'red', 'const
  
nexxo = new HydroCar nexxo '', skyblue '', '
console
log tesla instanceof ElecCar
console
log nexxo instanceof ElecCar
console
log nexxo instanceof Car
console
log nexxo instanceof Object
Quiz,
  console.log 는 어떻게 찍힐까요