function
  solution babbling )
let
  answer = 0
const
  words = aya '', ' ye', ' woo', '
const
  padArr = babbling map el )
for
let i = 0; i < words length ; i++)
const
  padStr = ' repeat words i length
el
  = el replace words i ], padStr
}
return
el
};
console
log padArr
const
  resultArr = padArr map el ) el replace ((/ g ,
console
log resultArr
console
log resultArr filter el ) el ===
  answer
= resultArr filter el ) el === length
return
answer
}